import { HeroSection, HeroTitle } from "./HeroStyles";

const Hero = () => (
  <HeroSection>
    <div className="container">
      <HeroTitle>
        As melhores vagas para tecnologia, design e artes visuais.
      </HeroTitle>
    </div>
  </HeroSection>
);

export default Hero;
